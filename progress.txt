# Ralph Progress Log
Branch: main
Started: Thu Jan 15 10:10:39 PM EST 2026
---

## Codebase Patterns
- The library uses optional peer dependencies for editor add-ons (e.g. `modern-monaco`, `@opencode-ai/sdk`), with config toggles in runtime config.
- `index.html` defines demo UI elements (like AI chat controls).
- `examples/quickstart.ts` wires UI IDs to `init()` config.
- AI chat behavior is centralized in `src/core/init.ts` and `src/core/aiChat.ts`.
- Server-side sync helpers live under `src/server` and are exported from `index.ts`.
- Page metadata helpers (`PageMetaStore`, adapters) live in `src/server` for bun/sqlite and Cloudflare durable objects.
- Frontend sync uses storage keys prefixed with `<pageKey>:` to store per-file content alongside a `meta` entry.
- `examples/quickstart.ts` runs in browser context and expects `document` to exist.
- `src/core/iframeCanvas.ts` owns WYSIWYG iframe styling and event handling.

## Thu Jan 15 22:50:00 EST 2026 - 2025-12-19-supertab-dl4
- Added sqlocal-based storage adapter and config type for OPFS-backed storage.
- Documented sqlocal storage setup and optional peer dependency.
- Files changed: `src/core/StorageManager.ts`, `src/types.ts`, `index.ts`, `package.json`, `README.md`, `src/types/sqlocal.d.ts`.
- **Learnings for future iterations:**
  - Optional storage adapters should be surfaced as peer dependencies.
  - Storage config lives in both `src/core/StorageManager.ts` and `src/types.ts`.
  - `README.md` is the place for usage examples of new adapters.

## 2026-01-15 23:53 - 2025-12-19-supertab-7gv
- Added AI chat model selection dropdown + model routing to chat prompts.
- Files changed: `src/types.ts`, `src/core/aiChat.ts`, `src/core/init.ts`, `examples/quickstart.ts`, `index.html`, `progress.txt`.
- **Learnings for future iterations:**
  - `bun run build` is the configured test script; lint/typecheck scripts are absent.
  - `examples/quickstart.ts` requires a browser context (DOM) and fails under Bun CLI.

## Thu Jan 15 11:59:00 PM EST 2026 - 2025-12-19-supertab-qes
- Adjusted AI chat streaming to attach to message part events immediately and filter by session to avoid waiting for completion.
- Files changed: src/core/aiChat.ts, progress.txt.
- **Learnings for future iterations:**
  - AI chat streaming listens to opencode SSE message.part.updated events; ensure session filtering doesn’t delay deltas.
---

## Thu Jan 15 11:12:42 PM EST 2026 - 2025-12-19-supertab-z8n
- Added server page metadata helpers with Bun SQLite and Cloudflare Durable Object adapters.
- Exported new server helpers and demonstrated metadata creation in the quickstart example.
- Files changed: `src/server/sync.ts`, `src/server/bun_server.ts`, `src/server/cf_worker.ts`, `index.ts`, `examples/quickstart.ts`, `progress.txt`.
- **Learnings for future iterations:**
  - Page meta storage is handled by `PageMetaStore` adapters in `src/server` for bun/sqlite and CF durable objects.
  - `createPageMeta` converts `PageData` into metadata payloads for server persistence.
---

## Thu Jan 15 11:31:24 PM EST 2026 - 2025-12-19-supertab-7p4
- Added a frontend sync helper that mirrors workspace files via storage adapters.
- Wired quickstart to demonstrate syncing local files with a mock server adapter.
- Files changed: `src/server/sync.ts`, `src/types.ts`, `index.ts`, `examples/quickstart.ts`, `progress.txt`.
- **Learnings for future iterations:**
  - Use `syncFrontendWithServer` with a `ServerSyncAdapter` to pull/push file lists against storage adapters.
  - Sync metadata is stored as `<pageKey>:meta` so clients can detect newer server state.
---

## Thu Jan 15 11:57:11 PM EST 2026 - 2025-12-19-supertab-45t
- Added Drizzle schema definitions for pages and page files to support sqlite-backed storage.
- Exposed the schema via the package entrypoint and added drizzle as an optional peer dependency.
- Files changed: `src/server/schema.ts`, `index.ts`, `package.json`, `progress.txt`.
- **Learnings for future iterations:**
  - Use the Drizzle schema in `src/server/schema.ts` for sqlite-backed page storage.
---

## Fri Jan 16 00:56:00 EST 2026 - 2025-12-19-supertab-g0c
- Added server sync utilities for websocket page payloads in Bun and Cloudflare Worker environments.
- Exported sync message helpers and types from the library entrypoint.
- Documented server sync usage in README and noted helpers in quickstart.
- Files changed: `src/server/sync.ts`, `src/server/bun_server.ts`, `src/server/cf_worker.ts`, `src/types.ts`, `index.ts`, `README.md`, `examples/quickstart.ts`, `progress.txt`.
- **Learnings for future iterations:**
  - Server sync messages are normalized to a `page` payload with simple ack responses.
  - Use `createBunSyncServer`/`createCfSyncWorker` for websocket integration in backend runtimes.

## 2026-01-16 00:02:51 EST - 2025-12-19-supertab-re8
- Added hover-only box model overlay to show margin/padding on canvas components.
- Files changed: `src/core/iframeCanvas.ts`.
- **Learnings for future iterations:**
  - Box model overlays are best implemented inside the iframe WYSIWYG CSS/script.
  - Hover interactions can be wired via `initWYSIWYG` listeners on component elements.
---
