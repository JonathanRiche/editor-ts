{
  "name": "@editorts/desktop",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:native": "bash ./scripts/dev-native.sh",
    "dev:native:shell": "bun x electrobun dev",
    "build": "bun run build:native",
    "build:web": "vite build",
    "build:native": "bunx electrobun build",
    "build:canary": "bunx electrobun build --env=canary",
    "build:stable": "bunx electrobun build --env=stable",
    "preview": "vite preview"
  },
  "dependencies": {
    "@editorts/starter-shared": "workspace:*",
    "editor-ts": "workspace:*",
    "electrobun": "^1.15.1",
    "solid-js": "^1.9.0"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite": "^7.3.1",
    "vite-plugin-solid": "^2.11.10"
  }
}
